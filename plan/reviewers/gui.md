# GUI reviewer — checklist

Named on every task that closes a GUI-area requirement or any `GUI screenshot` verify (`plan/WORKFLOW.md` § "Task files"). The
authority order is: rulings in `decisions.md`, then `GUI_DESIGN_NOTES.md`, then the twelve artboards; the notes win over
the pictures and a ruling wins over both (`docs/gui/design/GUI_DESIGN_NOTES.md` § "Dev GUI (egui) — design notes for the artboards"; `docs/gui/principia_render_gui_spec.md` § "Principia — Dev GUI (egui / F3)").

## 1. Rules that hold everywhere

- [ ] egui is a debug layer toggled with F3 over the wgpu render, sharing the engine's `wgpu` context and surface; it has the final UI's structure and every feature. `docs/gui/design/GUI_DESIGN_NOTES.md` § "Rules that hold everywhere"; `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"
- [ ] Every control reads a `Snapshot` and sends a typed `SetField`; data flows one way, UI → `SetField` → core → snapshot → UI, and no control touches simulation internals. `docs/gui/design/GUI_DESIGN_NOTES.md` § "Rules that hold everywhere"; `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"
- [ ] Navigation is chart construction: pan, zoom and slice edit `z₀` and the basis; no control, window, log line or type is named after a camera. `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"; `docs/gui/principia_render_gui_spec.md` § "G13. Where the artboards are overridden"
- [ ] Every preview of the slice keeps the viewport's aspect (square for the slice); none stretches or crops it. `docs/gui/design/GUI_DESIGN_NOTES.md` § "Rules that hold everywhere"; `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"
- [ ] The figure is never covered: no toasts or labels over the plot; warnings go to the footer console. The hover label and lock reticle are the only marks on the figure. `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"
- [ ] Dark default egui theme, Ubuntu / Ubuntu Mono, no styling beyond egui's own. `docs/gui/design/GUI_DESIGN_NOTES.md` § "Rules that hold everywhere"
- [ ] No Chazy subtitles and no object-based stain desk; Stain is the plain node-graph editor. `docs/gui/design/GUI_DESIGN_NOTES.md` § "Rules that hold everywhere"
- [ ] Each control knows whether its field is sim key or render key and warns per field from the caching blast-radius table, not from the struct the field lives in. `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"; `docs/contracts/principia_gui_state_contract.md` § "2. The editable state is the entire coupling surface"; `docs/contracts/principia_caching_contract.md` § "Part 2 — What invalidates what (the dependency graph)"
- [ ] Explore and Stain are the two modes; everything else is a window or a tool; Run settings live in the Run window, not on the page. `docs/gui/principia_render_gui_spec.md` § "G14. Settled by the notes (record)"
- [ ] The outcome legend is called "Legend", never "Fate". `docs/gui/principia_render_gui_spec.md` § "G6. Legend — generated by evaluating the stain (`06_legend.png`)"; `docs/gui/principia_render_gui_spec.md` § "G13. Where the artboards are overridden"

## 2. The state contract: firewall

- [ ] The gui crate depends only on the engine's typed state surface (`SimConfig` / `RenderState` / `ViewUI`, `SetField`, the snapshot); scheduler, cache, kernel and payload buffers are not reachable from it. `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"; `docs/contracts/principia_canonical_spec.md` § "1. The substrate (the defining decision)"
- [ ] The engine never imports the GUI; the dependency arrow is GUI → state → engine. `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"
- [ ] egui holds no `&mut` into engine state even though it is in-process; it earns no shortcut the TS binding would not have. `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"
- [ ] The boundary is data, not objects: no live handle to an engine struct is kept by the GUI, only the last snapshot. `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"; `docs/design/principia_systems_architecture.md` § "3. The membrane — the deployment view (demoted, not diminished)"
- [ ] The snapshot is GUI-sized (view state, tier, a few scalars), throttled to ~10 Hz; egui redraws at frame rate from the latest one; no payload, tree or reduction array is in it. `decisions.md` § "R-94 — The GUI snapshot is ~10 Hz *(closes RQ-45)*"; `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"
- [ ] No GUI element holds sim logic, caches sim data or computes anything the engine needs. `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"
- [ ] The engine could be driven from a JSON of `SimConfig + RenderState` with no GUI attached (the PR's GUI feature has a non-GUI path to the same edit). `docs/contracts/principia_gui_state_contract.md` § "1. The one-way dependency rule"
- [ ] The precision warning and console read what the snapshot and telemetry stream carry (`DECODE_SWITCHOVER` on visible quads, `AT_F32_FLOOR`), not fixed depths. `decisions.md` § "R-54 — The precision warning is event-driven *(GU-3 (b))*"; `docs/contracts/principia_gui_state_contract.md` § "2. The editable state is the entire coupling surface"
- [ ] Tier-3 overlays upload only per-visible-quad metadata (~16 KB design budget) while on; nothing per-sample crosses; finer detail goes to inspector drill-down. `docs/gui/principia_render_gui_spec.md` § "12.1 Structural overlays and tile debug shaders"

## 3. The state contract: where state lives

- [ ] Each new field is placed as the contract says: chart, `z₀ q₁ q₂`, slice values, lock flag, `z_locked`, excursion `δ`, link ids, integrator occupant, `T`/`dt`/thresholds/`eps`, `r_coll`, quality → `SimConfig`; stain graph, overlays, palette/compaction, playhead `t` → `RenderState`; panels, focus, selection, kept orbits, `t_cursor`, debug visibility, transport, linked views → `ViewUI`. `docs/contracts/principia_gui_state_contract.md` § "2. The editable state is the entire coupling surface"
- [ ] Lock is chart construction and lives in `SimConfig`. `decisions.md` § "R-69 — What is undoable *(closes the step-6 open question)*"; `docs/gui/principia_render_gui_spec.md` § "G4. Lock — the reticle and the pin (`08_lock.png`)"
- [ ] Transport (play / pause / speed / loop) is `ViewUI`: not undoable, not on the sim key. `decisions.md` § "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"; `decisions.md` § "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
- [ ] The link ids in `SimConfig` are the chart's link functions; linked side-by-side views are a separate `ViewUI` item. `decisions.md` § "R-106 — The link ids are the chart's link functions *(closes RQ-66)*"
- [ ] The engine reads no `ViewUI` field. `docs/contracts/principia_gui_state_contract.md` § "2. The editable state is the entire coupling surface"; `docs/contracts/principia_gui_state_contract.md` § "7. What a replacement GUI must honour (the teardown contract)"
- [ ] Quality is the preset selector (Auto / named tiers / Custom) editing `SimConfig.quality`; Custom is pre-filled with the current preset and read-only until touched; the arbiter stays engine-side. `docs/contracts/principia_gui_state_contract.md` § "6. Quality settings — preset selector over one struct (see `principia_quality_device_note.md`)"
- [ ] The fragment registry is the scanned `frag/` directories (a file appearing registers it); `debug/` is a category tag filtered by `ViewUI`, not a separate mechanism; compute occupants are a choice among compiled variants. `docs/contracts/principia_gui_state_contract.md` § "3. The registry is the scanned filesystem — for the *fragment* side; compute occupants are Rust build variants"
- [ ] Slots are typed only by output signature (`colour` → `vec3`, `brightness` → `f32`); no rule forces "vector → colour, scalar → brightness". `docs/contracts/principia_gui_state_contract.md` § "4. The occupant model — typed by signature, free inside"
- [ ] A categorical value is never averaged; mixed pixels resolve colour-per-sample then SSAA. `docs/contracts/principia_gui_state_contract.md` § "4. The occupant model — typed by signature, free inside"

## 4. Undo and redo

- [ ] Undo/redo is one history of typed `SetField` edits kept by the contract and shared by every GUI; the GUI keeps none of its own and sends undo/redo as requests. `decisions.md` § "R-52 — Undo lives in the state contract *(GU-1 (a))*"; `docs/contracts/principia_gui_state_contract.md` § "2. The editable state is the entire coupling surface"
- [ ] Every `SimConfig` and `RenderState` edit is undoable, including navigation (edits of `z₀` and the basis) and lock / unlock. `decisions.md` § "R-69 — What is undoable *(closes the step-6 open question)*"
- [ ] `ViewUI`-only state (open panels, focus, selection, kept orbits, transport) never enters the history. `decisions.md` § "R-69 — What is undoable *(closes the step-6 open question)*"; `decisions.md` § "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
- [ ] A drag coalesces into one history entry; a manual scrub is one coalesced entry. `decisions.md` § "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"; `decisions.md` § "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
- [ ] Playback advances `RenderState`'s playhead each frame through a `SetField` marked "no history" and never enters undo. `decisions.md` § "R-101 — Transport lives in `ViewUI`; the clock writes the playhead without history *(closes RQ-61)*"
- [ ] The top bar shows undo / redo depth from the contract, and Ctrl+Z undoes from the contract's history. `docs/gui/principia_render_gui_spec.md` § "G1. Rules that hold everywhere"; `docs/gui/principia_render_gui_spec.md` § "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"

## 5. Screen-specific rules the pictures can't carry

- [ ] "Manifold view" is one group (chart, navigation, centre `z₀`, slice and tilt, rotation); chart presets are named by their axes, never nicknames. `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"; `docs/gui/design/GUI_DESIGN_NOTES.md` § "01 Explore — the everyday view"
- [ ] The precision warning is raised by events, never tied to fixed depths. `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"; `decisions.md` § "R-54 — The precision warning is event-driven *(GU-3 (b))*"
- [ ] Under lock, sliders are re-based to the anchor (anchor + offset), not frozen; the compass shows the gold pin; tilt, rotation, zoom and chart changes turn about it; affine charts compute `z_locked` directly and nonlinear charts replay Φ and D on the CPU. `docs/gui/principia_render_gui_spec.md` § "G4. Lock — the reticle and the pin (`08_lock.png`)"; `docs/gui/design/GUI_DESIGN_NOTES.md` § "08 Lock — the reticle and the pin"
- [ ] The hover path is `z(t)` projected onto the chart's `q₁, q₂` (solid near the slice, faint away), and the hover label names a fate only once decided ("undecided · still interacting at t of t_max" before). `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"
- [ ] Picking (hover, click, lock) flips pointer Y the same way the render does before decoding. `docs/design/principia_coordinate_conventions_note.md` § "Code paths that must honour the single convention (each is a separate path)"; `docs/design/principia_trajectory_viewing.md` § "1. The single mechanism"
- [ ] No trace is drawn over the stale backdrop (no current-identity payload under the cursor → no trace). `docs/contracts/principia_render_contract.md` § "Part 7 — The hover trace (per-IC trajectory overlay)"
- [ ] The time scrubber sets the display time and re-integrates progressively, says so ("refining · N%"), and never replays stored frames. `decisions.md` § "R-66 — The time scrubber stays *(closes RQ-22)*"; `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"
- [ ] The compass switches mode by itself (slice vs tilt) and, when locked, turns the plane about the pin. `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"; `docs/gui/principia_render_gui_spec.md` § "G14. Settled by the notes (record)"
- [ ] The Run window exposes the parameters the contracts define under their contract names (`T_horizon`, `dt_macro`, `N_max` default 64, `r_sub`/`gamma_sub`, `r_coll`, `r_close`, `eps_E`/`eps_L`, occupant, `tau`, escape window, quality, frame budget, `MAX_REL_DEPTH`, `E`); there is no persistence count and no integrator "tolerance" field. `decisions.md` § "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"; `docs/gui/principia_render_gui_spec.md` § "Run — from the top bar"
- [ ] The Profiler's top level is telemetry §2's five stages; the artboard's finer categories are nested scopes. `decisions.md` § "R-56 — Profiler schema v1 is a superset of telemetry §2, in JSON *(GU-5, amended)*"; `docs/gui/principia_render_gui_spec.md` § "Profiler"
- [ ] The Display window applies stain → style → display scale → gamut clamp → colour-vision simulation → screen, in that order; style applies to the figure only; scientific checks run with plain. `decisions.md` § "R-67 — The display chain *(closes RQ-23)*"; `docs/gui/principia_render_gui_spec.md` § "Display — the last stages"
- [ ] Colour-vision simulation is real Viénot (protan, deutan) and Brettel (tritan) through LMS. `decisions.md` § "R-78 — Real Viénot and Brettel colour-vision simulation *(closes RQ-29)*"; `docs/design/principia_colour_composition.md` § "4.3 Display stage (terminal, outside the pipeline)"
- [ ] The Overlays menu and the Display window replace the global display bar; each overlay is implemented in the tier §12.1's sorting rule assigns (post node / sim shader / tile debug shader). `decisions.md` § "R-67 — The display chain *(closes RQ-23)*"; `docs/gui/principia_render_gui_spec.md` § "12.1 Structural overlays and tile debug shaders"
- [ ] The legend is generated by walking back from OUT, sampling each colour and brightness node over its declared input domain (inherited from the manifest by default) and pushing through the real combiner, with one key per dimension. `decisions.md` § "R-53 — Node interfaces declare their input domains *(GU-2 (a))*"; `docs/gui/principia_render_gui_spec.md` § "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
- [ ] The outcome legend uses colour_composition §1.4's canonical palette and 0-based labels; `running` shows neutral grey and `sim_failed` the invalid colour. `decisions.md` § "R-77 — Replace-L, and the state palette *(closes RQ-28)*"; `decisions.md` § "R-22 — Body indices are 0-based; pair ids name the opposite side *(CD-2, amended)*"; `decisions.md` § "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"
- [ ] Chart builder: each axis has a kind; a physical-quantity axis makes the chart nonlinear; the Domain preview comes from the chart's `validate(u, v)`; both previews are square; the footer checks `q₁ · q₂ = 0`. `docs/gui/principia_render_gui_spec.md` § "G7. Chart builder (`03_chartbuilder.png`)"; `decisions.md` § "R-26 — Every chart declares its domain function *(CD-6)*"
- [ ] The Inspector is one window (IC, canonical representative, real-space trajectory; one timeline; editing the IC re-integrates on the CPU in f64 through `computeIC`); right-click on a body opens its properties popover; disc radius ∝ ∛m. `decisions.md` § "R-65 — One Inspector window; the standalone IC Inspector is absorbed *(closes RQ-21)*"; `decisions.md` § "R-96 — Colour and GUI definitions *(closes RQ-52 and RQ-54, definitional parts)*"; `docs/gui/principia_render_gui_spec.md` § "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
- [ ] "listen" follows pointer_channels §4 only: pitch scaled from the crossing time, cross-fade and never retrigger; the reference pitch is a calibration and the θ/φ and windowing choices are definitions. `decisions.md` § "R-109 — Only pointer_channels §4 is normative *(closes RQ-70)*"; `docs/design/principia_scratchpad_pointer_channels.md` § "4. Audio-specific constraints, which are NOT the frame budget"
- [ ] Recording a sweep integrates each frame to its own `t` with the export contract's blocking barrier; importing a picture reads its pxpack and shows what differs from now. `docs/gui/principia_render_gui_spec.md` § "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"; `docs/contracts/principia_export_animation_contract.md` § "Part 4 — Export: the frame loop in blocking mode"
- [ ] Measure owns `FULL_RETENTION`: a uniform grid with every sample kept, never quadtree leaves. `decisions.md` § "R-39 — `FULL_RETENTION` keeps bit 4, owned by the measurement path *(PL-4, amended)*"; `docs/gui/principia_render_gui_spec.md` § "G10. Measure — a tool on the figure (`10_measure.png`)"
- [ ] Keyboard: a tree of scopes in the numbered Tab order; Enter in, Esc out; Shift ×10, Alt ×0.1; DAS/ARR repeat; only a focus ring and the breadcrumb show. `docs/gui/principia_render_gui_spec.md` § "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
- [ ] The console is the same stream as the profiler's telemetry; each `SetField` is logged; errors open it automatically. `docs/gui/principia_render_gui_spec.md` § "G12. Console (`12_console.png`)"

## 6. The stain editor (Part II)

- [ ] `OUT` and `combiner` are fixed singletons that cannot be added, deleted or duplicated; the tail is `… → combiner → (post)* → OUT`; the interior is free subject to port types and acyclicity. `docs/gui/principia_render_gui_spec.md` § "4. The backbone — fixed vs free"; `decisions.md` § "R-64 — The stain editor is a free, typed node graph *(closes RQ-20)*"
- [ ] A wire attaches only if port types match; `field` subtype is not a hard gate (the inspector morphs); an in-port takes one wire (last write wins); out-ports fan out. `docs/gui/principia_render_gui_spec.md` § "6. Port typing & wire rules"
- [ ] The graph is always renderable: dangling inputs evaluate as occupant identity (None), and an invalid graph shows a defined fallback, never a crash. `docs/gui/principia_render_gui_spec.md` § "13. Invariants"; `docs/design/principia_colour_composition.md` § "4.1 Backbone & `Option` occupants"
- [ ] Every colouring maps NaN or a sentinel to its invalid colour; debug fields show literal stored values (NaN still to the invalid colour). `decisions.md` § "R-79 — NaN and sentinels *(closes RQ-30)*"; `docs/gui/principia_render_gui_spec.md` § "13. Invariants"
- [ ] A custom node compiles async with last-valid-pipeline fallback and per-node failure isolation, with a compile-status indicator and error surface. `docs/contracts/principia_gui_state_contract.md` § "5. The stain editor — a free, typed node graph (R-64)"
- [ ] `Graph | Code` is a representation toggle: graph view is offered iff the pipeline still has graph structure; there is no eject dialog. `docs/gui/principia_render_gui_spec.md` § "10. `Graph | Code` — a view toggle, not an eject"
- [ ] A preset is a whole serialised graph that replaces the current one; debug presets load locked and fork a custom copy on first edit. `docs/gui/principia_render_gui_spec.md` § "11. Presets = whole graphs"; `docs/design/principia_colour_composition.md` § "6. Debug views as presets"
- [ ] Display-stage settings are settings, never nodes, and apply uniformly to the main render and the preview. `docs/gui/principia_render_gui_spec.md` § "12. Display stage — global, outside the pipeline"
- [ ] The post chain is bounded at ≤ 8 nodes. `docs/design/principia_colour_composition.md` § "4.2 Post chain"

## 7. Screenshots against the artboards

The artboards set **layout only**; their values are illustrative and corpus values win (palette hex codes, `N_max` default
64, the sound mapping), and the Run window uses contract names (`decisions.md` § "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"; `docs/gui/principia_render_gui_spec.md` § "G13. Where the artboards are overridden").

- [ ] Each `cargo xtask screenshot <artboard>` run compares layout (regions, grouping, order, presence of controls) against the artboard, never pixel values, colours or numbers. `decisions.md` § "R-68 — Artboard values are illustrative; corpus values win *(closes RQ-24)*"; `plan/WORKFLOW.md` § "The unit: one task, one branch, one PR"
- [ ] Where the screenshot differs from the artboard on a value, label or control, the difference is one §G13 lists (0-based labels; no persistence count; profiler five stages; no camera; "Legend" not "Fate"; palette, `N_max`, sound mapping; no "tolerance" field) or one a later ruling makes — otherwise it is a finding. `docs/gui/principia_render_gui_spec.md` § "G13. Where the artboards are overridden"
- [ ] A surface with no artboard is checked by presence only, not layout, until the M8 dev GUI: the custom quality fields in the Run window under "quality: Custom", the target-utilisation ceiling in the Run window, the arbiter overlay in a Profiler tab, and the passive-logging Profiler switch with its footer indicator. `decisions.md` § "R-129 ✱ — Where the surfaces with no artboard live *(closes RQ-105)*"
- [ ] Screenshots run on GUI PRs and at the gates, rendered headless with native wgpu offscreen. `decisions.md` § "R-110 — What CI runs, where, and against which goldens *(closes RQ-79)*"
- [ ] Where the artboard and GUI_DESIGN_NOTES disagree, the screenshot follows the notes; where either disagrees with a ruling, it follows the ruling, and any notes edit is marked "conformed to R-n". `docs/gui/principia_render_gui_spec.md` § "Principia — Dev GUI (egui / F3)"; `decisions.md` § "R-107 — Apply the RQ-67 follow-ups; GUI_DESIGN_NOTES may be conformed *(closes RQ-67)*"

Artboard → governing section:

- [ ] `docs/gui/design/01_main.png` — Explore, the everyday view → `docs/gui/principia_render_gui_spec.md` § "G2. Explore — the everyday view (`01_main.png`)"
- [ ] `docs/gui/design/02_stain.png` — Stain, the node-graph editor → `docs/gui/principia_render_gui_spec.md` § "Part II — The stain editor"
- [ ] `docs/gui/design/03_chartbuilder.png` — Chart builder → `docs/gui/principia_render_gui_spec.md` § "G7. Chart builder (`03_chartbuilder.png`)"
- [ ] `docs/gui/design/04_windows.png` — Profiler, Export & share, Display, Run → `docs/gui/principia_render_gui_spec.md` § "G5. Windows (`04_windows.png`)"
- [ ] `docs/gui/design/05_inspectors.png` — Inspector → `docs/gui/principia_render_gui_spec.md` § "G8. Inspector — one IC, its trajectory, one timeline (`05_inspectors.png`)"
- [ ] `docs/gui/design/06_legend.png` — Legend → `docs/gui/principia_render_gui_spec.md` § "G6. Legend — generated by evaluating the stain (`06_legend.png`)"
- [ ] `docs/gui/design/07_keyboard.png` — Keyboard (a design note, not a screen: check the scope order and focus ring, not a screen layout) → `docs/gui/principia_render_gui_spec.md` § "G3. Keyboard — a design note, not a screen (`07_keyboard.png`)"
- [ ] `docs/gui/design/08_lock.png` — Lock → `docs/gui/principia_render_gui_spec.md` § "G4. Lock — the reticle and the pin (`08_lock.png`)"
- [ ] `docs/gui/design/09_importrecord.png` — Import picture, saved views, record a sweep → `docs/gui/principia_render_gui_spec.md` § "G9. Import picture · saved views · record a sweep (`09_importrecord.png`)"
- [ ] `docs/gui/design/10_measure.png` — Measure → `docs/gui/principia_render_gui_spec.md` § "G10. Measure — a tool on the figure (`10_measure.png`)"
- [ ] `docs/gui/design/11_research.png` — Research, first pass (v2) → `docs/gui/principia_render_gui_spec.md` § "G11. Research — first pass (v2) (`11_research.png`)"
- [ ] `docs/gui/design/12_console.png` — Console → `docs/gui/principia_render_gui_spec.md` § "G12. Console (`12_console.png`)"

<!-- list:gui-screenshots -->
*65 requirements, generated from `plan/requirements.yaml` — do not edit by hand.*

**M1**
- [ ] REQ-TOOL-010 — each view selectable in the debug picker renders on a synthetic payload

**M3**
- [ ] REQ-TOOL-132 — the |n|−1 view is flat zero on a real march; the effort heatmap shows the march's per-pixel substep counts

**M6**
- [ ] REQ-GUI-014 — presence only (R-129; no layout comparison until the M8 dev GUI): the Run window's 'quality: Custom' section shows each control
- [ ] REQ-TOOL-058 — presence only (R-129; no layout comparison until the M8 dev GUI): the Profiler tab shows the four items

**M7**
- [ ] REQ-COL-022 — remove the brightness node: it shows ghosted; restore restores; undo covers the SetField
- [ ] REQ-COL-026 — enable deutan: every surface including thumbnails is simulated; no preset serialises CVD
- [ ] REQ-COL-044 — screenshots of each v1 preset (plain, watercolour & pencil, the seven print presets) against 04_windows.png's Display window; chrome and legend are unstyled
- [ ] REQ-GUI-019 — Node WGSL / Pipeline WGSL views show the generated code
- [ ] REQ-GUI-020 — controls present on each Artefact-1 map
- [ ] REQ-GUI-031 — Against the stain-editor artboard; unit test that arbitrary typed wiring is accepted and type-mismatched wiring rejected.

**M8**
- [ ] REQ-CHART-040 — screenshot of each axis kind against 03_chartbuilder.png; normalise sets |q| = 1
- [ ] REQ-RENDER-073 — hover over a stale backdrop region: no trace; over a current region: trace drawn above the composite
- [ ] REQ-GUI-034 — slider/direction labels distinguish 'one mass logit' from 'only m₀'
- [ ] REQ-GUI-041 — dial at top detent selects nearest; support toggle switches vMF/Voronoi family
- [ ] REQ-GUI-042 — preview label shows the mass point; inspecting a pixel changes it
- [ ] REQ-GUI-044 — presence (R-129): the utilisation control is in the Run window; default leaves headroom; the record names the ceiling
- [ ] REQ-GUI-045 — attempt a Yellow and a Red change; dialogs show the stated content and allow proceeding; no warning appears on current state
- [ ] REQ-GUI-047 — select High: fields show 16² samples, depth 6, E = 3, FTLE on greyed; edit E → mode shows Custom with the other values kept
- [ ] REQ-GUI-055 — screenshot against 05_inspectors.png's readouts
- [ ] REQ-GUI-057 — screenshot: canonical arrows are labelled/drawn as momenta
- [ ] REQ-GUI-058 — screenshot of both panes against 05_inspectors.png
- [ ] REQ-GUI-059 — screenshot with a large and a small configuration: both fit; the stamp is present
- [ ] REQ-GUI-066 — screenshot of the readout panel against 05_inspectors.png
- [ ] REQ-GUI-069 — one screenshot per screen compared for layout with 01_main.png … 12_console.png (values are illustrative, R-68); the mode switch offers exactly Explore / Stain
- [ ] REQ-GUI-070 — screenshots with F3 on and off against 01_main.png: the figure is identical underneath; review that egui-wgpu is constructed from the engine's device/queue, not a second context
- [ ] REQ-GUI-073 — screenshots of 03_chartbuilder and 02_stain layouts: both chart-builder previews and the stain preview are square for a square viewport and match the viewport aspect otherwise
- [ ] REQ-GUI-074 — trigger a warning and an error with the figure visible: screenshot against 01_main.png shows nothing new over the plot; the footer count increments
- [ ] REQ-GUI-075 — screenshot against 01_main.png: dark theme, Ubuntu for text, Ubuntu Mono for numbers/code
- [ ] REQ-GUI-078 — screenshot against 01_main.png's top bar; force the budget to bind and check 'budget-bound' appears; make two edits and check the undo depth reads 2
- [ ] REQ-GUI-079 — screenshot of the open menu against 01_main.png; each Alt+digit toggles its overlay; 'all off' clears the overlay set
- [ ] REQ-GUI-080 — screenshots of Explore (01_main.png) and Stain (02_stain.png): no display bar; style / scale / gamut / CVD are reachable only from the Display window
- [ ] REQ-GUI-081 — screenshot against 01_main.png's left panel: one group, sub-sections in that order, eight named z₀ sliders
- [ ] REQ-GUI-082 — screenshot against 01_main.png; every shipped preset's display name is built from its axis names
- [ ] REQ-GUI-084 — screenshot against 01_main.png; after a pan the end values update
- [ ] REQ-GUI-085 — screenshot against 01_main.png: the path is drawn from the cursor's IC with opacity falling with distance from the slice
- [ ] REQ-GUI-088 — screenshot against 01_main.png's right panel; both buttons open the Inspector window
- [ ] REQ-GUI-089 — screenshots against 01_main.png and 05_inspectors.png in sphere and unwrapped modes; with 'turn' cleared two frames are identical
- [ ] REQ-GUI-091 — screenshots against 01_main.png after touching a slice slider and after touching a tilt, and against 08_lock.png when locked
- [ ] REQ-GUI-093 — screenshot against 01_main.png's footer; a click opens the 12_console.png layout
- [ ] REQ-GUI-094 — screenshot against 01_main.png shows none of these fields; all are in the Run window (04_windows.png)
- [ ] REQ-GUI-098 — screenshots before and after moving focus differ only in the ring and the breadcrumb (07_keyboard.png)
- [ ] REQ-GUI-099 — screenshot against 08_lock.png after K over a point
- [ ] REQ-GUI-100 — screenshot in present mode shows only the figure; Esc restores 01_main.png's layout
- [ ] REQ-GUI-101 — screenshot against 04_windows.png's Display window
- [ ] REQ-GUI-103 — screenshot against 04_windows.png's Run window shows tau and window, no 'persistence' field
- [ ] REQ-GUI-104 — screenshot against 04_windows.png; Cancel during a recompute stops it and the progress resets
- [ ] REQ-GUI-106 — screenshots of a bivariate stain and a log-ramp stain against 06_legend.png
- [ ] REQ-GUI-110 — screenshot against 03_chartbuilder.png
- [ ] REQ-GUI-113 — screenshot against 05_inspectors.png; dragging an arrow edits velocity
- [ ] REQ-GUI-114 — screenshots of both views and of the ghost animation's four stages against 05_inspectors.png
- [ ] REQ-GUI-115 — screenshot against 05_inspectors.png
- [ ] REQ-GUI-119 — screenshot against 05_inspectors.png's readout row
- [ ] REQ-GUI-121 — screenshot against 10_measure.png; the CSV has one row per sampled pair
- [ ] REQ-GUI-123 — screenshot against 11_research.png; on a continuation fixture the fold and period-doubling markers sit where a Floquet multiplier crosses +1 / −1
- [ ] REQ-GUI-124 — screenshot against 11_research.png; the section picker offers exactly the three sections
- [ ] REQ-GUI-125 — screenshot against 11_research.png; a pan in one side pans the other
- [ ] REQ-GUI-126 — screenshot against 12_console.png; raising an error opens it
- [ ] REQ-GUI-128 — screenshot against 02_stain.png
- [ ] REQ-GUI-129 — screenshot in both preview modes against 02_stain.png; an edit updates the preview
- [ ] REQ-GUI-135 — screenshot against 02_stain.png with one hand-edited and one selected node
- [ ] REQ-GUI-139 — screenshot against 02_stain.png's library; typing in the filter narrows rows
- [ ] REQ-GUI-142 — Run window screenshot lists contract parameter names; substep cap default reads 64; palette colours equal corpus hex codes.
- [ ] REQ-GUI-161 — with the shape-sphere chart, the Chart section shows both controls and each changes the figure; with any other chart neither is shown
- [ ] REQ-TOOL-088 — presence (R-129): the Profiler switch exists; the footer indicator is visible while passive logging is on; logging is off by default
- [ ] REQ-TOOL-098 — screenshot against 04_windows.png's profiler; the histogram's cap marker sits at N_max; the window reads the profiler file prin profile writes, and that file's header config is REQ-GUI-039's provenance object (R-113)
<!-- /list:gui-screenshots -->
